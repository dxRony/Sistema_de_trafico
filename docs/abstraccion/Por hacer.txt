					Por hacer:
- simulacion de trafico.
	* calcularComplejidad de cada interseccion	
	* mover a los vehiculos a su destino
- reportes en tiempo real.
- reportes de simulacion.




			Calculo de complejidad de rutas
- arbol de Interseccion
- cada interseccion se guarda como un nodo en un arbol avl.
- el valor del nodo representa el trafico o complejidad (vehiculos procesados, bloqueos).
- se permite la busqueda, insercion y reorganizacion de rutas en O(log n).
- el arbol se reequilibra para encontrar la ruta mas fluida

			Prioridad de vehiculos
1) tipo de vehiculo
2) nivel de urgencia
3) tiempo de espera

/home/ronyrojas/Escritorio/vehiculos.csv


System.out.println("******************************");
        for (int y = alto - 1; y >= 0; y--) {
            Node<T> fila = obtenerNodo(0, y);

            for (int x = 0; x < ancho && fila != null; x++) {
                Interseccion interseccion = (Interseccion) fila.getData();
                System.out.print(interseccion.getRepresentacionConsola() + " ");
                fila = fila.getRight();
            }
            System.out.println();
        }
        System.out.println("******************************");